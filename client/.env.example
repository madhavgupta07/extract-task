# API Configuration
# Copy this file to .env.local and update values for local development
# For production (Vercel), set VITE_API_URL in the Vercel dashboard environment variables
# Leave empty for production - the app will use relative paths ("")

VITE_API_URL=http://localhost:5000
